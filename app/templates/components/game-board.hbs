{{!-- {{#if deck}}
<button class='btn reset-game' {{action 'deleteGame'}}>Restart Game</button>
{{/if}} --}}

{{!-- {{#if over}} --}}
  <button class='btn play-again' {{action 'playAgain'}}>Play Again</button>
{{!-- {{/if}} --}}

{{!-- <div class='selected-card-container'>
{{#each selectedArray as |card|}}
  <img class="selected-card" src={{card.img}}>
{{/each}}
</div> --}}

<div class='col-md-12 card-container'>
{{#each gameArray as |card|}}
  {{game-board/card card=card
    toggleSelect='toggleSelect'}}
{{/each}}
</div>

{{outlet}}
